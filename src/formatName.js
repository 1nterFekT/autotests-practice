/**
 * @returns {string} Сокращенную строку
 * 
 * @description Функция получает на вход полное ФИО, и возвращает его сокращенную версию
 * 
 * @example
 * formatName("Иванов Иван Иванович") // "Иванов И.И."
 * formatName("   Петров Пётр Петрович ") // "Петров П.П."
 * formatName("Олегов Олег") // Error 
 */

export function formatName(fullName) {
    return ''
}